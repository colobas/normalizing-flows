from setuptools import setup

setup(name='normalizing-flows',
      version='0.0',
      description='Guilherme Pires MSc Thesis',
      author='Guilherme Pires',
      author_email='mail@gpir.es',
      packages=['normalizing_flows'],
      zip_safe=False)
